name: stark_bloc
description: Stark Bloc - Tools to use Stark in a Bloc project
version: 0.0.1
homepage: https://github.com/diefferson/stark_bloc

environment:
  sdk: '>=3.2.5 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  stark: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
